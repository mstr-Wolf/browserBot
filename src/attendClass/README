Background execution:
    nohup python3 -u main.py [meeting code] [class length] [target time] > log.out & cat attendClass.out
*class length must be in minutes
*target time format -> hour:minutes

Or execute through CLI:
    import sys
    sys.append("/path/to/browserBot/src")
    from attendClass import AttendClass
    object = AttendClass()
    object.run()